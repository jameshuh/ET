-- Generated by CSharp.lua Compiler
local System = System
local DCET
System.import(function (out)
  DCET = out.DCET
end)
System.namespace("DCET", function (namespace)
  namespace.class("Game", function (namespace)
    local eventSystem, getEventSystem, objectPool, getObjectPool, Close, class
    getEventSystem = function ()
      local default = eventSystem
      if default == nil then
        eventSystem = DCET.EventSystem()
        default = eventSystem
      end
      return default
    end
    getObjectPool = function ()
      local default = objectPool
      if default == nil then
        objectPool = DCET.ObjectPool()
        default = objectPool
      end
      return default
    end
    Close = function ()
      local default = class.Scene
      if default ~= nil then
        default:Dispose()
      end
      class.Scene = nil


      local default = objectPool
      if default ~= nil then
        default:Dispose()
      end
      objectPool = nil

      eventSystem = nil
    end
    class = {
      getEventSystem = getEventSystem,
      getObjectPool = getObjectPool,
      Close = Close
    }
    return class
  end)
end)
