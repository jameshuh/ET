-- Generated by CSharp.lua Compiler
local System = System
local DCETRuntime = DCETRuntime
System.namespace("DCET", function (namespace)
  namespace.class("TimeHelper", function (namespace)
    local CurrentLocalMilliseconds, CurrentLocalSeconds
    CurrentLocalMilliseconds = function ()
      return DCETRuntime.LocalTimeHelper.CurrentMilliseconds()
    end
    CurrentLocalSeconds = function ()
      return DCETRuntime.LocalTimeHelper.CurrentSeconds()
    end
    return {
      CurrentLocalMilliseconds = CurrentLocalMilliseconds,
      CurrentLocalSeconds = CurrentLocalSeconds
    }
  end)
end)
