-- Generated by CSharp.lua Compiler
local System = System
local DCET = DCET
local DCET
local ListIMHandler
local DictUInt16ListIMHandler
System.import(function (out)
  DCET = out.DCET
  ListIMHandler = System.List(DCET.IMHandler)
  DictUInt16ListIMHandler = System.Dictionary(System.UInt16, ListIMHandler)
end)
System.namespace("DCET", function (namespace)
  namespace.class("MessageDispatcherComponentAwakeSystem", function (namespace)
    local Awake
    Awake = function (this, t)
      t:Awake()
    end
    return {
      base = function (out)
        return {
          out.DCET.AwakeSystem_1(out.DCET.MessageDispatcherComponent)
        }
      end,
      Awake = Awake,
      __metadata__ = function (out)
        return {
          class = { 0x6, out.DCET.ObjectSystemAttribute() }
        }
      end
    }
  end)

  namespace.class("MessageDispatcherComponentLoadSystem", function (namespace)
    local Load
    Load = function (this, self)
      self:Load()
    end
    return {
      base = function (out)
        return {
          out.DCET.LoadSystem_1(out.DCET.MessageDispatcherComponent)
        }
      end,
      Load = Load,
      __metadata__ = function (out)
        return {
          class = { 0x6, out.DCET.ObjectSystemAttribute() }
        }
      end
    }
  end)

  -- <summary>
  -- 消息分发组件
  -- </summary>
  namespace.class("MessageDispatcherComponent", function (namespace)
    local Awake, Load, RegisterHandler, Handle, Dispose, class, __ctor__
    __ctor__ = function (this)
      this.handlers = DictUInt16ListIMHandler()
      DCET.Entity.__ctor__(this)
    end
    Awake = function (this)
      Load(this)
      class.Instance = this
    end
    Load = function (this)
      this.handlers:Clear()

      local types = DCET.Game.getEventSystem():GetTypes(System.typeof(DCET.MessageHandlerAttribute))

      for _, type in System.each(types) do
        local continue
        repeat
          local attrs = type:GetCustomAttributes(System.typeof(DCET.MessageHandlerAttribute), false)
          if #attrs == 0 then
            continue = true
            break
          end

          local iMHandler = System.as(System.Activator.CreateInstance(type), DCET.IMHandler)
          if iMHandler == nil then
            DCET.Log.Error1("message handle " .. type:getName() .. " 需要继承 IMHandler")
            continue = true
            break
          end

          local messageType = iMHandler:GetMessageType()
          local opcode = this:getParent():GetComponent(DCET.OpcodeTypeComponent):GetOpcode(messageType)
          if opcode == 0 then
            DCET.Log.Error1("消息opcode为0: " .. messageType:getName())
            continue = true
            break
          end
          RegisterHandler(this, opcode, iMHandler)
          continue = true
        until 1
        if not continue then
          break
        end
      end
    end
    RegisterHandler = function (this, opcode, handler)
      if not this.handlers:ContainsKey(opcode) then
        this.handlers:AddKeyValue(opcode, ListIMHandler())
      end
      this.handlers:get(opcode):Add(handler)
    end
    Handle = function (this, session, opcode, message)
      local actions
      local default
      default, actions = this.handlers:TryGetValue(opcode)
      if not default then
        DCET.Log.Error1("消息没有处理: opcode=" .. opcode)
        DCET.Log.Error1("消息没有处理: message=" .. DCET.JsonHelper.ToJson(message))
        return
      end

      for _, ev in System.each(actions) do
        System.try(function ()
          ev:Handle(session, message)
        end, function (default)
          local e = default
          DCET.Log.Error(e)
        end)
      end
    end
    Dispose = function (this)
      if this:getIsDisposed() then
        return
      end

      DCET.Entity.Dispose(this)

      class.Instance = nil
    end
    class = {
      base = function (out)
        return {
          out.DCET.Entity
        }
      end,
      Awake = Awake,
      Load = Load,
      RegisterHandler = RegisterHandler,
      Handle = Handle,
      Dispose = Dispose,
      __ctor__ = __ctor__
    }
    return class
  end)
end)
